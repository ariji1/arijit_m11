<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Arijit Mukherjee — Data Science Profile (iOS style)</title>
  <meta name="description" content="Arijit Mukherjee — AI/ML, LLMs, MLOps. Clean iOS-style resume/profile." />

  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <style>
    /* System font stack (iOS-like) */
    :root{
      --accent:#007aff; /* iOS blue */
      --muted:#8e8e93;
      --bg:#f2f2f7;
      --card:#ffffffcc;
      --glass: rgba(255,255,255,0.6);
      --glass-2: rgba(255,255,255,0.4);
      --radius:18px;
      --glass-blur: 12px;
    }

    html,body{height:100%;}
    body{
      margin:0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
      background: linear-gradient(180deg,#f7f7fb 0%, #eef0f6 100%);
      color:#0b0b0b;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:3rem 1rem;
    }

    .wrap{
      width:100%;
      max-width:980px;
      display:grid;
      grid-template-columns: 360px 1fr;
      gap:28px;
      align-items:start;
    }

    /* Left column - profile card */
    .profile-card{
      background: linear-gradient(180deg, rgba(255,255,255,0.9), rgba(250,250,250,0.75));
      border-radius:var(--radius);
      padding:28px;
      box-shadow: 0 10px 30px rgba(10,10,20,0.08);
      backdrop-filter: blur(var(--glass-blur));
      border: 1px solid rgba(10,10,20,0.04);
    }

    .avatar{
      width:120px;
      height:120px;
      border-radius:28px;
      overflow:hidden;
      margin-bottom:18px;
      box-shadow: 0 8px 20px rgba(10,10,20,0.08);
      border: 1px solid rgba(0,0,0,0.05);
    }

    .avatar img{width:100%;height:100%;object-fit:cover;display:block}

    h1{font-size:1.6rem;margin:0 0 6px 0;letter-spacing:-0.3px}
    .role{color:var(--muted);font-size:0.95rem;margin-bottom:12px}

    .contact{display:flex;flex-direction:column;gap:8px;font-size:0.95rem}
    .contact a{color:var(--accent);text-decoration:none}

    .stats{
      display:flex;gap:12px;margin-top:16px
    }
    .stat{background:var(--glass-2);padding:10px;border-radius:12px;flex:1;text-align:center}
    .stat b{display:block;font-size:1.1rem}

    /* Right column - content */
    .content-col{display:flex;flex-direction:column;gap:22px}

    .card{
      background:var(--card);
      border-radius:16px;
      padding:18px 20px;
      box-shadow: 0 6px 22px rgba(10,10,20,0.06);
      border:1px solid rgba(10,10,20,0.04);
    }

    .headline{display:flex;align-items:center;justify-content:space-between;margin-bottom:8px}
    .headline h2{font-size:1.05rem;margin:0}
    .tag{font-size:0.85rem;color:var(--muted);padding:6px 10px;border-radius:999px;background:linear-gradient(180deg, rgba(255,255,255,0.6), rgba(245,245,250,0.6));border:1px solid rgba(10,10,20,0.03)}

    .about p{color:#111827;line-height:1.5;margin:0}

    .skills{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px}
    .skill{background:#f1f5f9;padding:8px 12px;border-radius:999px;font-weight:600;color:#0b3a66;font-size:0.9rem;border:1px solid rgba(10,10,20,0.03)}

    .timeline{display:flex;flex-direction:column;gap:12px}
    .job{display:flex;flex-direction:column;padding:10px;border-radius:12px;background:linear-gradient(180deg,#ffffff, #fbfbfd);border:1px solid rgba(10,10,20,0.03)}
    .job small{color:var(--muted)}

    .projects-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px}
    .proj{padding:12px;border-radius:12px;background:linear-gradient(180deg,#fff,#fbfdff);border:1px solid rgba(10,10,20,0.03)}

    .cta{display:flex;gap:10px;margin-top:12px}
    .btn{padding:10px 14px;border-radius:12px;border:none;cursor:pointer;font-weight:700}
    .btn-primary{background:linear-gradient(90deg,var(--accent), #0047cc);color:#fff}
    .btn-ghost{background:transparent;border:1px solid rgba(10,10,20,0.06);color:#0b0b0b}

    footer{opacity:0.7;font-size:0.9rem;color:var(--muted);text-align:center;margin-top:6px}

    @media (max-width:900px){
      .wrap{grid-template-columns:1fr;max-width:720px}
      .avatar{margin:0 auto}
      .profile-card{display:flex;flex-direction:row;gap:16px;align-items:center}
      .profile-card .meta{flex:1}
    }
  </style>
</head>
<body>
  <main class="wrap" role="main">
    <aside class="profile-card" aria-label="Profile card">
      <div style="display:flex;flex-direction:column;align-items:center;text-align:center">
        <div class="avatar" aria-hidden="false">
          <img src="Professional_Pic.jpg" alt="Arijit Mukherjee - Professional Photo">
        </div>
        <div class="meta">
          <h1>Arijit Mukherjee</h1>
          <div class="role">Senior AI / Data Science Architect</div>
          <div class="contact">
            <div><a href="mailto:ariji1job@gmail.com">ariji1job@gmail.com</a></div>
            <div style="color:var(--muted)">Bangalore, India — Open to relocation</div>
          </div>
          <div class="stats" style="margin-top:16px">
            <div class="stat"><b>8+</b><small style="color:var(--muted)">Years</small></div>
            <div class="stat"><b>15+</b><small style="color:var(--muted)">Projects</small></div>
          </div>
          <div class="cta">
            <button class="btn btn-primary" onclick="document.getElementById('contact-form').scrollIntoView({behavior:'smooth'})">Hire / Contact</button>
            <button class="btn btn-ghost" onclick="document.getElementById('projects').scrollIntoView({behavior:'smooth'})">View Work</button>
          </div>
        </div>
      </div>
    </aside>

    <section class="content-col">
      <div class="card about" aria-labelledby="about-title">
        <div class="headline"><h2 id="about-title">About</h2><span class="tag">Data Science • LLMs • MLOps</span></div>
        <p>I design and deliver production-grade AI systems — from LLM-based RAG pipelines and fine-tuning to scalable ML infra and observability. Focused on pragmatic, measurable impact.</p>
      </div>

      <div class="card">
        <div class="headline"><h2>Skills</h2><span class="tag">Core</span></div>
        <div class="skills">
          <span class="skill">Python</span>
          <span class="skill">LLMs & Transformers</span>
          <span class="skill">AWS / Azure</span>
          <span class="skill">SageMaker</span>
          <span class="skill">MLOps</span>
          <span class="skill">Databricks</span>
        </div>
      </div>

      <div id="projects" class="card">
        <div class="headline"><h2>Selected Projects</h2><span class="tag">Production</span></div>
        <div class="projects-grid">
          <div class="proj">
            <strong>Medical Coding RAG</strong>
            <div style="margin-top:6px"><small class="muted">Agentic LLMs, DPO, PHI-safe fine-tuning</small></div>
          </div>
          <div class="proj">
            <strong>Model Performance Tracker</strong>
            <div style="margin-top:6px"><small class="muted">Dashboards, drift detection, CI/CD</small></div>
          </div>
          <div class="proj">
            <strong>Time Series Forecasting</strong>
            <div style="margin-top:6px"><small class="muted">Demand forecasting with Prophet & ensembles</small></div>
          </div>
        </div>
      </div>

      <div class="card">
        <div class="headline"><h2>Experience</h2><span class="tag">Recent</span></div>
        <div class="timeline">
          <div class="job"><strong>HCL Technologies / AbbVie</strong><small>Senior Data Scientist • Jul 2024 — Present</small><div style="margin-top:6px;color:var(--muted)">LLM-Ops, large-scale pipelines, medical coding automation</div></div>
          <div class="job"><strong>Accenture</strong><small>Data Science Consultant • 2021 — 2024</small><div style="margin-top:6px;color:var(--muted)">RAG frameworks, sustainability extraction</div></div>
        </div>
      </div>

      <div class="card" id="contact-form">
        <div class="headline"><h2>Contact</h2><span class="tag">Let's connect</span></div>
        <form onsubmit="event.preventDefault();alert('Thanks — I will reach out.');" style="display:grid;gap:8px">
          <input type="text" placeholder="Your name" style="padding:12px;border-radius:10px;border:1px solid rgba(10,10,20,0.06)">
          <input type="email" placeholder="Email" style="padding:12px;border-radius:10px;border:1px solid rgba(10,10,20,0.06)">
          <textarea placeholder="Message" rows="4" style="padding:12px;border-radius:10px;border:1px solid rgba(10,10,20,0.06)"></textarea>
          <div style="display:flex;gap:8px">
            <button class="btn btn-primary" type="submit">Send Message</button>
            <a class="btn btn-ghost" href="mailto:ariji1job@gmail.com">Email directly</a>
          </div>
        </form>
      </div>

      <footer>Designed with an iOS-inspired aesthetic • Arijit Mukherjee</footer>
    </section>
  </main>

</body>
</html>
